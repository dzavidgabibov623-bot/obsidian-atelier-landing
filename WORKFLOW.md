# Safe Workflow

`main` is the stable deployed version.

Before a noticeable change:

1. create a working branch from `main`;
2. make and verify changes there;
3. merge back to `main` only after review.

Quick start:

```bash
./scripts/start-change.sh task-name
```
